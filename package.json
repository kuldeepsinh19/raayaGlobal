{
  "name": "raaya-global",
  "private": true,
  "description": "Agricultural export website for Raaya Global Solutions, with an AI product assistant, natural-language search, and auto-drafted buyer replies.",
  "license": "MIT",
  "scripts": {
    "dev": "cd client && npm run dev",
    "dev:api": "cd client && npm run dev:api",
    "build": "cd client && npm run build",
    "typecheck": "cd client && npm run typecheck",
    "preview": "cd client && npm run preview"
  }
}
